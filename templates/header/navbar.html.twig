<ul class="navbar-nav">
    <li class="nav-item {% if current_menu is defined and current_menu == "home"  %}active{% endif %} ">
        <a class="nav-link" href="{{ path('home') }}">Home</a>
    </li>
    <li class="nav-item {% if current_menu is defined and current_menu == "annonce"  %}active{% endif %}">
        <a class="nav-link" href="{{path('property.annonces')}}">Buy</a>
    </li>
    <li class="nav-item">
        <a href="{{ path('admin.property.create') }}" class="btn btn-primary text-caps btn-rounded btn-framed">Submit
            Ad</a>
    </li>
    <li class="nav-item {% if current_menu is defined and current_menu == "contact"  %}active{% endif %}">
        <a class="nav-link" href="{{ path('contact') }}">Contact</a>
    </li>
</ul>